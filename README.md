# 3dview

Static 3D building viewer for editing floor counts and per-floor side, number, latitude, and longitude assignments.

Open `index.html` locally or enable GitHub Pages for the repository. The viewer loads `9-356-364.city.json` automatically and supports downloading edited assignments as JSON.